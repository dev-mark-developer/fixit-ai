# API / Endpoint / Screen Changes Needed

Shared memory between you and me. Whenever UI work reveals that an **API,
endpoint, or screen with API integration** needs a change, add an entry here
**instead of** changing the API code (see [INSTRUCTIONS.md](./INSTRUCTIONS.md)).

Screen-level summary lists:
[SCREENS_API_CHANGES_REQUIRED.md](./SCREENS_API_CHANGES_REQUIRED.md) ·
[SCREENS_NO_API_CHANGES.md](./SCREENS_NO_API_CHANGES.md)

API reference: Swagger UI at `https://beta.contentdevelopmentpros.com:4125/swagger`
(spec: `/swagger/v1/swagger.json`, "FixitAI API v1", 99 paths). Note: the spec
documents request bodies but most **response schemas are untyped** ("200 OK"),
so response-shape questions still need backend confirmation.

> Status legend: 🔴 Open · 🟡 In discussion · 🟢 Resolved (kept for history)

| # | Date | Screen / File | Endpoint(s) | What's needed | Why | Status |
|---|------|---------------|-------------|---------------|-----|--------|
| _example_ | 2026-06-29 | `src/screens/Example.tsx` | `GET /example` | Endpoint should return `imageUrl` | UI needs to show a thumbnail | 🔴 Open |
| 1 | 2026-06-30 | Penpal "All" tab (Discover) | `GET /penpal/discover` | `discover` response should include a per-user **connection status** (e.g. `connectionStatus: None/Pending/Connected`) | Figma "All" tab shows a green **Connected** badge vs blue **Connect** per person. Discover currently returns no status. Workaround: client-side merge of `discover` + `getConnections` (approximate). | 🟢 Resolved 2026-08-17 |
| 2 | 2026-06-30 | Penpal Public Profile header | `GET /penpal/discover` · `GET /penpal/connections` · `GET /penpal/profile` | Penpal profile/connection responses should include **age** (or birthdate) | Figma header shows "Molly1522, **22**". No age/birthdate field exists in any penpal type today. | 🟢 Resolved 2026-08-17 |
| 3 | 2026-06-30 | Penpal Public Profile – "Letters Exchanged" | NEW e.g. `GET /penpal/letters?withUserId={id}` (today: client-side filter of `GET /penpal/letters`) | (Nice-to-have) a per-penpal **conversation** endpoint | Figma profile shows the letter thread with one penpal. Achievable client-side by filtering `getLetters` by counterpart id — no API change strictly required, but a dedicated endpoint would be cleaner/paginated. | 🟢 Resolved 2026-08-17 |
| 4 | 2026-07-02 | Mentor Profile Setup (`MentorProfileSetupScreen`) | `POST /mentor/profile` | `mentorApi.saveProfile` should accept **firstName, lastName, title, phone, dateOfBirth, country, city, state, and a profile image**. Currently it only takes `{displayName, bio, tagline}`. | Figma "Ready To Begin?" form collects all those fields. UI now renders them, but only displayName (=first+last), tagline (=title), bio (=about) are submitted; **phone / DOB / country / city / state / photo are collected but NOT saved** until the endpoint accepts them. | 🟢 Resolved 2026-08-17 |
| 5 | 2026-07-02 | Mentor Subscription price | `GET /subscription/status` + store IAP product **`com.monthly`** | Confirm the real plan price / IAP product. | Figma shows **$20/Month**. ~~`com.fixit.mentor.monthly`~~ — **superseded 2026-08-27**: mentor and dating share a single product, `com.monthly`, and one entitlement. The screen now reads the live localised `displayPrice` off the StoreKit product, so the `$20` label is only the fallback when the product can't be read. | 🟢 Resolved 2026-08-27 |
| 6 | 2026-07-03 | Dating Discover filters (`DatingDiscoverScreen`) | `GET /dating/discover` | `/dating/discover` should accept **country, distance, ageMax/ageRange, interestedInGender, interestIds** params | Figma has a Filter sheet (country, distance & age sliders, gender, premium Advance Filters). Built the full sheet UI; "Apply" currently only filters the already-loaded page client-side (country + age). Distance can't work at all without server support. | 🟢 Resolved 2026-08-17 |
| 7 | 2026-07-03 | Dating "Likes Received" / "My Likes" tabs (`DatingMatchesScreen`) | NEW e.g. `GET /dating/likes/received` · `GET /dating/likes/sent` | Endpoints to list **who liked me** and **who I liked** (likes-received gated by premium) | Figma My Matches has 3 tabs. Only `GET /dating/matches` exists. "Likes Received" shows the locked premium preview; "My Likes" shows an empty state until endpoints exist. | 🟢 Resolved 2026-08-17 |
| 8 | 2026-07-03 | Dating My Profile (`DatingMyProfileScreen`) | `POST /dating/profile` · `GET /users/me` (+ login response) for gender | `POST /dating/profile` should accept **pseudoName, dateOfBirth, country, city, state**; auth user should expose **gender** (badge) | Figma profile form collects all of these + a spiritual pseudonym. Today only `about` / `interestedInGender` / `displayImageId` are saved — the rest are collected but NOT submitted. Gallery (upload/delete) works with existing endpoints. | 🟢 Resolved 2026-08-17 |
| 9 | 2026-07-03 | Vetting result (`VettingQuizScreen`) | `POST /dating/vetting/submit` | `POST /dating/vetting/submit` should return **eligibility (pass/fail)** | Figma has both outcomes: "Congratulations! Your Profile is Approved" (pg 20) and "Sorry! …" + mentors (pg 14). The app currently always shows the Sorry/mentor path because the response isn't consumed. Response shape needs confirming before wiring the approved path. | 🔴 Open |
| 10 | 2026-07-03 | Dating chat attachments (`DatingChatDetailScreen`) | `POST /api/Dating/matches/{matchId}/uploads` (multi, key `files`) · `POST .../upload` (legacy single) + SignalR `SendMessageWithAttachments` | Send **image / video / voice** messages — app-side integration of the upload endpoints | ~~Upload endpoint needed~~ ~~Swagger check 2026-07-09~~ **Integrated 2026-08-18 against `docs/CHAT_API.md`.** Both open questions are answered by that spec: upload does **not** broadcast — the client uploads first, then invokes `SendMessageWithAttachments` over SignalR; and `audio/*` **is** accepted (classified as `VoiceNote`). App now uploads up to 10 files per message, sends text+files in one message, records voice notes (`react-native-nitro-sound`), and renders `attachments[]` with the legacy `fileUrl` fallback. | 🟢 Resolved 2026-08-18 |
| 11 | 2026-07-03 | Dating Premium (`DatingPremiumScreen`) | `GET /subscription/status` · `POST /subscription/restore` · store IAP product `com.monthly` | Dating IAP product + a **daily-swipe-limit signal** from the API | Figma: $20/Month premium plan + "You Have Reached Your Daily Limit!" overlay on Discover. **Implemented 2026-08-27 (iOS only)** — real StoreKit 2 purchase via `react-native-iap`, Restore Purchases, and the limit overlay driven by a **402/403 from `POST /dating/swipe`** (agreed with the client rather than reading a count from `/Dating/config`). Cancel now points at Apple's subscription settings, since Apple owns cancellation for auto-renewing products. Android keeps the "coming soon" alert until Play billing is wired up. | 🟢 Resolved 2026-08-27 |

| 12 | 2026-08-17 | Profile images app-wide (seen on `DatingMatchesScreen` → My Likes) | Static hosting for `profileImageUrl` paths (e.g. `/profiles/{userId}/{guid}.png`) returned by `GET /dating/likes/sent` and other profile endpoints | **Uploaded profile images are not reachable.** Serve the stored paths (or return absolute URLs). | The likes response returns `"profileImageUrl": "/profiles/90/3e3859dd-….png"`. The app resolves relative paths against the API origin, but that URL **404s** — verified against `https://beta.contentdevelopmentpros.com:4125` and the `/api`, `/uploads`, `/files`, `/media`, `/images`, `/static`, `/wwwroot` prefixes plus the port-443 host. Either static file middleware isn't configured for `/profiles`, the file was never written, or images live on another host the app hasn't been told about. Client-side the URL handling is correct; cards now fall back to initials instead of a blank photo. **Seen again 2026-09-17 (`GET /external-mentors`, Vetting Quiz "Choose a Mentor"):** two of the six external mentors' images 404 (`/external-mentors/fb83acc2-….png`, `/external-mentors/cb2ab52c-….png`), and Mentor Buhaha's is a 1×1 PNG, so it shows as a plain colour dot. The app shows initials for the missing ones. | 🔴 Open |

| 13 | 2026-08-17 | Dating Profile Detail opened from Likes (`DatingMatchesScreen` → `DatingProfileDetailScreen`) | `GET /dating/user/{userId}` (full public dating profile) | A way to fetch **another user's full dating profile** by id | ~~No "get another user's profile" endpoint exists in Swagger today.~~ **Backend added `GET /dating/user/{userId}`; integrated 2026-08-24.** `DatingProfileDetailScreen` now fetches the full profile on open (route params act as instant seed data), so About / Interest chips / Gallery render regardless of whether the screen was opened from Discover or from My Likes / Likes Received. Response fields are read defensively (`datingApi.getUser`, `DatingUserDetail`) since the shape isn't typed in Swagger. | 🟢 Resolved 2026-08-24 |

| 14 | 2026-08-18 | Dating Chat header presence (`DatingChatDetailScreen`) | SignalR hub `/hubs/chat` — NEW e.g. `GetOnlineUsers` invoke, or `isOnline`/`lastSeenAt` on `GET /dating/matches` | A **presence roster**: a way to learn who is *already* online when you connect | `docs/CHAT_API.md` states it outright: "presenceRoster: none — UserOnline only fires on connect, joiners miss who is already online". So opening a chat with someone who connected earlier shows **no status at all** until they send a message or reconnect. The header now renders Online/Offline only once a live event proves it, rather than guessing — but the correct fix is server-side. | 🔴 Open |

| 15 | 2026-08-18 | Dating Chat voice notes (`DatingChatDetailScreen`) | `POST /api/Dating/matches/{matchId}/uploads` response · `attachments[]` on messages | Attachments should carry a **`durationMs`** (audio/video length) | A voice-note bubble can't show its length until playback starts, because nothing in the payload says how long the clip is. The app caches the duration after the first play as a workaround, but a fresh history load shows "Voice note" with no time. Same field would let video tiles show a duration badge. | 🔴 Open |

| 16 | 2026-08-18 | Dating Chat (`DatingChatDetailScreen`) | SignalR hub `/hubs/chat` — `UserOnline` / `UserOffline` scope | `UserOnline`/`UserOffline` should be scoped to the user's **matches**, not broadcast to everyone | `docs/CHAT_API.md`: "Broadcast to **every** connected user, not only matches." Every client is woken for every login in the system — needless traffic that grows with the user base, and it leaks who is online to people with no relationship to them. | 🔴 Open |

| 17 | 2026-08-18 | Dating Chat header avatar (`DatingChatDetailScreen`) | NEW e.g. `GET /dating/matches/{matchId}` | A **single-match** endpoint | To draw the peer's avatar the screen fetches the entire `GET /dating/matches` list and finds one row (pre-existing behaviour, kept). Wasteful once a user has many matches. Low priority — cosmetic-path only. | 🔴 Open |

| 18 | 2026-08-18 | Dating chat attachments (`DatingChatDetailScreen`) | `POST /api/Dating/matches/{matchId}/uploads` · `POST .../upload` | Match the documented `image/*` · `video/*` · `audio/*` prefixes instead of an exact list — at minimum add **`image/jpg`**, **`audio/x-m4a`** and **`image/heic`/`image/heif`**, and make the match case-insensitive | The endpoint docs promise `image/*`, `video/*`, `audio/*`, but it enforces a fixed, **case-sensitive** list. Verified against beta 2026-08-18: **accepted** = `image/jpeg`, `image/png`, `image/webp`, `video/mp4`, `video/quicktime`, `audio/m4a`, `audio/aac`, `audio/mpeg`; **rejected** = `image/jpg`, `image/pjpeg`, `image/JPEG`, `image/heic`, `image/heif`, `image/gif`, `video/mov`, `video/mpeg4`, `video/x-m4v`, `video/3gpp`, `video/webm`, `audio/x-m4a`, `audio/mp3`, `audio/mp4`, `audio/wav`, `audio/ogg`, `audio/3gpp`, `audio/amr`, `audio/x-caf`. Three hurt: **`image/jpg`** is what `react-native-image-picker` reports for *every* JPEG (it concatenates a sniffed extension onto `"image/"`), so the commonest attachment of all was refused — canonicalised client-side in `src/services/chatAttachments.ts`; **`audio/x-m4a`** is what iOS derives for a `.m4a` voice note (RN overwrites the declared type with a UTI-derived one), so **every iPhone voice note failed** — worked around in `src/utils/uploadPart.ts` by sending audio as a base64 form part so the declared `audio/m4a` survives; and **`image/heic`** is the default iPhone photo format, currently dodged only because the picker re-encodes to JPEG. | 🔴 Open |

| 19 | 2026-08-20 | Push notifications (app-wide) | `POST /api/Auth/heartbeat` · `POST /api/Auth/logout` · NEW e.g. `DELETE /api/Auth/device/{deviceId}` | A way to **deregister** a device's push token, and confirmation that heartbeat is the intended token-refresh channel | Registering works: `pushToken` on login/register covers sign-in, and `POST /auth/heartbeat { deviceId, pushToken }` covers rotation mid-session — that's what the app now uses (see [PUSH_NOTIFICATIONS.md](./PUSH_NOTIFICATIONS.md)). **Deregistering has no route.** `POST /auth/logout` takes no body, and heartbeat needs a live session, so once a user signs out there's no authenticated call left that can clear their token. The app deletes the FCM token locally on logout — delivery stops — but the backend keeps a stale row that will fail forever. Also worth confirming heartbeat is *meant* to be used this way rather than purely for presence. | 🔴 Open |

| 20 | 2026-08-27 | In-app purchases (app-wide) | `POST /auth/login` response | Login should return a **per-user GUID** for Apple's `appAccountToken` | Apple requires `appAccountToken` to be a **UUID**, but `data.userId` is an int, and this value is how the App Store webhook resolves a transaction back to an account. **Backend added `identifier` (e.g. `CF5696FE-F6F3-44BE-897E-6FD6956FE4CE`) — wired 2026-08-27.** The derived stand-in (`00000000-0000-4000-8000-<userId padded>`) is kept only for sessions saved before the field existed, which have no `identifier` until the user signs in again. ⚠️ **One thing left for the backend: compare case-insensitively.** StoreKit takes a `UUID` and Apple renders it **lower-case** in the signed transaction, so the webhook will receive `cf5696fe-…` while the stored value is upper-case. The app sends it lower-cased so both ends use one spelling, but a naive `==` against the stored upper-case value would still fail — and the failure is silent: the purchase succeeds and entitlement never arrives. | 🟡 Verify |

| 21 | 2026-08-27 | In-app purchases (app-wide) | `GET /api/Subscription/status` | Publish the **response schema** (and confirm the field names) | Swagger documents this as a bare "200 OK", so the app can't know what it returns. `src/api/subscription.ts` normalises defensively — it accepts `isActive`/`isSubscribed`/`hasActiveSubscription`, falls back to `isExpired`, then to a `status` string (`Active`/`Trial`/`GracePeriod`/`Cancelled` count as entitled, per the IAP doc's access-control table), and treats an `expiresAt`/`expiryDate`/`expirationDate`/`endDate` in the past as expired regardless. A confirmed shape would let all that guesswork go. **Live shape seen 2026-09-11:** `{ isEntitled, status, planType, productId, store, environment, startDate, expirationDate, autoRenewEnabled, isTrialPeriod, accessUntil }` (free account: `isEntitled: false`, `status: "None"`). The normaliser doesn't read `isEntitled` or `accessUntil` yet — it gets the answer from `status` and `expirationDate`. | 🔴 Open |

| 22 | 2026-08-27 | Dating Discover swipe limit (`DatingDiscoverScreen`) | `POST /api/Dating/swipe` | Answer **402 or 403** (as agreed with the client) once an account's daily **swipes** or **super likes** are spent — or at least add an error code | This is what raises the "You Have Reached Your Daily Limit!" overlay. **Verified live 2026-09-11 (`sp3@yopmail.com`, free):** the endpoint *does* enforce the limits, but refuses with **400** and only a message — the 2nd super like of the day got `400 "You have reached your daily limit of 1 super likes. Upgrade to Premium for more."`. 400 is also used for validation errors, so the client now treats 402/403 **or a 400 whose message says "daily limit"** as the limit — a stopgap that breaks if the wording changes. The limit applies to *the action that was refused*: a refused Like → cover + ♥ dimmed; a refused SuperLike → alert + ⭐ dimmed, and likes keep working (also verified: a Like right after the refusal went through); a pass is never treated as limited. The card goes back (the refused swipe didn't count — verified), and the block lifts at local midnight or on a premium purchase. Still unverified: the 11th swipe, whether a pass counts toward the 10, and the reset time. Full ask: gap #33 / [BACKEND_LIKE_LIMITS.md](./BACKEND_LIKE_LIMITS.md). **Tested 2026-09-14 (`nons@yopmail.com`):** despite the backend update, the super-like refusal is still `400` with only the message — no `code` — so the client still relies on 402/403 or the "daily limit" wording. Any other swipe failure puts the card back and shows the server's message in a toast (since 2026-09-15). | 🔴 Open |

| 23 | 2026-08-27 | Likes Received premium gate (`DatingMatchesScreen`) | `GET /api/Dating/likes/received` | Confirm the endpoint returns **402/403** for non-premium accounts | The locked preview (blurred tiles + "Get a Premium Access") is driven entirely by that status — the client deliberately does **not** pre-lock from its own entitlement state, so the backend stays the single source of truth. If the endpoint instead returns 200 with an empty list, free users will see "No Likes Received" rather than the paywall. **Checked live 2026-09-11: it is not gated.** `sp3@yopmail.com` (free, `isEntitled: false`) got **200 with the full list** — the person who super-liked them, name and photo included — so free users see Likes Received unlocked. Needs fixing server-side (or the client pre-locks after all, which is a product call). **2026-09-16: the client now pre-locks** (the user's call): free accounts see up to two of their likers' photos, blurred, under a padlock, and 402/403 still locks too. That is cosmetic only — the response still hands a free account every liker's name and unblurred photo URL, so the server-side gate is still needed. When it lands, a 402/403 shows plain dark tiles, unless the backend returns something to blur (e.g. blurred thumbnails or a count). | 🔴 Open |

| 24 | 2026-08-27 | In-app purchases — webhook latency (app-wide) | `POST /api/Subscription/webhook/ios` → `GET /api/Subscription/status` | Confirm the **typical delay** between Apple's server notification and `status` flipping to active | Per the agreed design the app posts nothing on purchase — it polls `status` for **30s** (2s interval) behind an "Activating your subscription…" spinner, then shows a "taking longer than usual" state with a Check Again button. The StoreKit transaction is deliberately left **unfinished** until entitlement arrives, so a purchase whose webhook never lands replays on the next launch instead of being lost. If 30s is routinely too short, that window should be raised. | 🔴 Open |

| 25 | 2026-08-28 | Dating type switching (`DatingLobbyScreen` · `NonSpiritualEntryScreen`) | NEW e.g. `DELETE /api/Users/me` (account deletion) | An **account-deletion endpoint** the app can call | Product rule: a **Spiritual account can never switch to Non-Spiritual** — switching requires deleting the account and registering again. The app now enforces the block (the Non-Spiritual card is locked in the lobby, and `NonSpiritualEntryScreen` refuses a Spiritual profile even if reached directly) and tells the user what to do, but it **cannot offer the way out**: nothing in Swagger deletes a user, and `usersApi` only has `GET`/`PATCH /users/me` + profile-image upload. Until the endpoint exists the user has to be told to contact support@fixit.com. Also worth confirming server-side that `POST /dating/profile` **rejects** a `datingType` change on an existing Spiritual profile — the client guard is not a security boundary. | 🔴 Open |

| 26 | 2026-08-28 | Find a Kindred Spirit — Requests/Connected tabs (`PenpalConnectionsScreen`) | `GET /api/Penpal/discover?status=Pending\|Accepted` | Discover rows should carry the **connection** behind them: `connectionId`, `requesterId`/`receiverId` and the request's `createdAt` | All three tabs now call `discover` with `status` (empty for All, `Pending`, `Accepted`) instead of Requests and Connected using `GET /penpal/connections`. Discover returns the *person*, but the Requests tab acts on the *connection*: it needs the id to Accept/Decline/Cancel, and which end sent the request to decide which of those to show. Swagger types the discover response as a bare "200 OK", so the app reads these fields defensively and, when they're absent, backfills them from one `GET /penpal/connections?status=` call per load. That extra call disappears on its own once discover includes them. | 🔴 Open |

| 27 | 2026-09-08 | Dating type switching — Non-Spiritual → Spiritual (`HomeScreen` · `DatingLobbyScreen` · `SpiritualEntryScreen`) | `POST /api/Dating/profile` | Confirm the endpoint **accepts a `datingType` change from `NonSpiritual` to `Spiritual`** on an existing profile, and say what it does to the profile's existing interests | A Non-Spiritual member can now reach the lobby from Home and move over to Spiritual once vetted. "Start Spiritual Dating" re-POSTs the existing profile with `datingType: 'Spiritual'` (about / display image / DOB / location sent back so the switch keeps them), then routes through Spiritual interest selection because `/dating/interests` is per dating type. If the endpoint **rejects** the change, or keeps the old Non-Spiritual `interestIds` next to the newly picked ones, the account ends up half-migrated — the client shows the server's message but can't repair it. The reverse direction stays blocked client-side (gap #25). | 🔴 Open |

| 28 | 2026-09-09 | Dating My Profile gallery (`DatingMyProfileScreen`) | `GET /api/Dating/profile` (or a config endpoint) · `POST /api/Dating/images` | Expose the admin panel's **Max Gallery Images** value to the client, and name the limit in the refusal | QA (Asana "Max Gallery Images limit not properly enforced"): with the admin limit set to 3, uploading a 4th image just fails. The app can now surface whatever the endpoint says instead of its own blanket "could not upload" text, so a refusal that reads "Maximum 3 images are allowed" already reaches the user — but the app still **cannot disable the Add button up front**, because no endpoint returns the limit. Any field on the profile response (e.g. `maxGalleryImages`) would do. Same for the accepted image types and max file size: the client currently pre-checks a **5 MB** ceiling and blocks GIF/HEIC from the verified chat allow-list (gap #18), which is an assumption, not a published contract. **Live 2026-09-11:** `GET /api/Dating/config` returns `{ "maxGalleryImages": 4 }`, so the limit *is* exposed — the app just doesn't read it yet. | 🔴 Open |

| 29 | 2026-09-09 | Blocking (`DatingMatchesScreen` · `DatingChatsScreen` · `DatingChatDetailScreen`) | `GET /api/Dating/matches` · `POST /api/blocks` · SignalR `/hubs/chat` | (a) `matches` should **exclude blocked users**, (b) confirm blocking does **not delete** the match/chat, and (c) the hub should **refuse messages** between blocked users | QA (Asana: "Blocked user still appears in Matches and chat remains active" · "Blocked user chat should show blocked status instead of being deleted"). Blocked people are already filtered out of Likes Received / My Likes but still come back from `GET /dating/matches`, so the app now cross-references `GET /blocks` client-side and hides them; the chat row stays in the list, dimmed, reading "You blocked this user", and the composer is replaced by a notice. Three things the client cannot settle: **(a)** the filtering belongs server-side — with it, the extra `/blocks` call per screen goes away; **(b)** if the backend *does* delete the match on block, the thread vanishes from `matches` and no client change can keep the history the ticket asks for — needs confirming either way; **(c)** the composer guard is UX, **not** a security boundary: a blocked pair must be rejected by the hub/API too, and blocking only covers people *this* user blocked — being blocked *by* someone is invisible to the client and can only be enforced server-side. | 🔴 Open |

| 30 | 2026-09-09 | Timestamps app-wide (Requests · Notifications · Online Letter Exchange · Matches · Letters) | Every endpoint returning a date-time (`createdAt`, `sentAt`, `matchedAt`, `blockedAt`, `reviewedAt`, …) | Serialise date-times as **ISO-8601 with a zone marker** (`2026-09-09T09:12:44.601Z`), as the SignalR hub already does | QA (Asana: two "5 hours ago" tickets). REST bodies send `"2026-09-09T09:12:44.601"` with no zone. `new Date()` reads that as *local*, so on a +05:00 device — PKT, where the team and QA are — every row came out exactly five hours older than it is: a request created seconds ago rendered as "5 hours ago" and appeared frozen there. Already documented for chat in [CHAT_API.md](./CHAT_API.md) ("REST history has NO zone marker - parse as UTC"); it is true of the whole API. **Worked around client-side 2026-09-09** — `src/utils/datetime.ts` tags bare timestamps as UTC before parsing, and every screen goes through it. Worth fixing at the source anyway: any new client (web admin included) hits the same trap, and the workaround silently guesses UTC for a value that does not say so. | 🔴 Open |

| 31 | 2026-09-10 | Notifications (`NotificationsScreen`) | `PATCH /api/notifications/{id}/toggle-read` | An **idempotent** "mark as read" (e.g. `PATCH /notifications/{id}/read`) rather than a toggle | QA (Asana: "Notification dot reappears after being read"). The only endpoint is a toggle, so a second call flips a read notification back to unread — which is what the dot reappearing was. The app now only calls it when the row is unread, which fixes the reported case, but a toggle is still the wrong primitive: two devices (or a retry after a timeout the server actually processed) tap the same notification and the second call marks it unread again. An idempotent read — and ideally a "mark all read" for the module — removes the race entirely. | 🔴 Open |

| 32 | 2026-09-10 | Dating chat voice notes (`DatingChatDetailScreen` · `chatAttachments`) | `POST /api/Dating/matches/{matchId}/uploads` · SignalR `SendMessageWithAttachments` | Say what happens when **one request carries two or more `audio/*` parts**, and whether the hub accepts **more than one `VoiceNote` attachment** on a single message | QA (Asana: "Voice notes sent as empty message when sending multiple at once"). One voice note works; two or more in a single message arrive as an **empty message**. Everything the client sends looks correct — unique file names, `audio/m4a` preserved through the base64 workaround (gap #18), each descriptor carrying a `fileUrl`, and the hub client refuses to send an attachment without one. That leaves two candidates the app cannot see into: the endpoint mishandling several audio parts in one multipart body, or the hub rejecting multiple `VoiceNote` attachments per message and storing an empty row. **Resolved client-side 2026-09-11** — a finished recording is now sent as its own message the moment it stops, instead of waiting in the composer tray, so a message can never carry two voice notes and the broken case no longer exists. (The earlier workaround — uploading several audio parts as separate requests — was removed as dead code.) The API question stands for its own sake: if the endpoint or hub does mishandle two audio attachments in one message, any future feature that batches them hits it again. Low priority now that no client path does. | 🟡 Client-side resolved |

| 33 | 2026-09-11 | Dating Discover like limits (`DatingDiscoverScreen` · `NonSpiritualEntryScreen`) | `POST /api/Dating/swipe` · `GET /api/Dating/config` · `GET`/`PUT /api/admin/settings` | Refuse with a **distinct error code**, return **super-like counters + `resetsAt`** next to the swipe counters, expose the same from `GET /api/Dating/config`, and confirm whether a **pass** counts toward the daily swipes | Full write-up: [BACKEND_LIKE_LIMITS.md](./BACKEND_LIKE_LIMITS.md). Live (2026-09-11) the swipe response already carries `dailySwipeLimit` / `swipesUsedToday` / `swipesRemainingToday` / `isPremium` (undocumented), but nothing for super likes and no reset time, and `/Dating/config` returns only `maxGalleryImages`. So the app can't know the limits before the first swipe, can't dim ♥/⭐ up front, and still hardcodes "10 free swipes / 1 super like per day" on the Non-Spiritual entry screen. The refusal is a plain 400, so the app has to recognise it by its wording. **Update 2026-09-14:** `/Dating/config` now returns `freeSwipesPerDay` / `freeSuperLikesPerDay` / `premiumSwipesPerDay` / `premiumSuperLikesPerDay` (verified), and the Non-Spiritual entry screen shows those instead of the hardcoded numbers. **Tested later the same day (`nons@yopmail.com`):** the swipe response now carries `dailySuperLikeLimit` / `superLikesUsedToday` / `superLikesRemainingToday` and `resetsAt` (midnight UTC), and the app uses them to dim ♥/⭐ and lift the block at the reset. But **a pass uses up a swipe** (1 → 2), against the requirement that passing is unlimited, and the refusal still has no code (gap #22). Still missing: today's usage in config and Swagger types. **Passes, decided 2026-09-14:** they count toward the daily swipes, and the app now blocks ✕ at the limit along with ♥/⭐. | 🟡 Partly done |

| 34 | 2026-09-17 | In-app purchases split per module (`MentorSubscriptionScreen` · `DatingPremiumScreen` · every premium gate) | `POST /api/Subscription/webhook/ios` · `POST /api/Subscription/restore` · `GET /api/Subscription/status` · `POST /api/Dating/swipe` · `GET /api/Dating/likes/received` | (a) Map each **product id to its module**: `com.monthly` → Mentor; `com.nonspiritual.monthly` / `com.spiritual.monthly` → Dating. (b) Make `status` return **one entitlement per group** (e.g. `entitlements: [{ group, productId, isEntitled, expirationDate, … }]`, or `?group=`). (c) Dating premium (swipe limits, likes received, the swipe response's `isPremium`) must count **only the dating products**; the mentor gate only `com.monthly` | Two App Store subscription groups since 2026-09-17: **Mentor** (`com.monthly`) and **Dating** (both dating products; a user is only ever one dating type). The app now buys the product for the screen it's on and unlocks a module only when `status.productId` belongs to that module's group. **Until (b):** `status` describes one subscription, so a mentor who also buys dating premium holds two, and the app can only see whichever one the backend returns — the other module shows as locked. A `status` with no `productId` still unlocks everything, so a payer is never locked out. **Until (c):** the backend's own limits treat any subscription as dating premium, so a mentor-only subscriber gets premium swipe limits. Old sandbox purchases of `com.monthly` made for dating now count as Mentor only. | 🔴 Open |

| 35 | 2026-09-17 | Assigned Mentor details (`AssignedMentorScreen`, opened from the Mentor Assigned card on `SpiritualEntryScreen`) | `GET /api/mentor-request` | Add the assigned mentor's **title, bio and email** to the response as `assignedMentorTagline`, `assignedMentorBio`, `assignedMentorEmail` (the app already reads these names), or add an endpoint a member can call for their assigned mentor's profile | Figma "Mentor Details" shows the mentor's title ("Professor at Harvard"), an About section and a Contact Details email. **Checked live 2026-09-17 (`sp4@yopmail.com`, assigned mentor 202):** `mentor-request` returns only `id, userId, status, assignedMentorId, assignedMentorName, assignedMentorDisplayName, assignedMentorImageUrl, assignedAt, createdAt`. No other endpoint a member can call has the mentor's profile: `GET /Dating/user/202` → 404 (mentors have no dating profile), `GET /Mentor/profile` → 403 (mentor role, own profile only), `/admin/users/{id}` and `/admin/mentor/list` → 403. Until then those three parts are hidden. Also to confirm: the design's **Registration Date** is shown as the date the member asked for a mentor (`createdAt`); say if it should be the mentor's own registration date instead (that would need another field). Side note: mentor 202's photo is a 2.7 MB 1920×1080 PNG (~30 s to download on beta); resizing profile uploads server-side would help every avatar. | 🔴 Open |

| 36 | 2026-09-17 | Upload Certificate (`UploadCertificateScreen`) | `POST /api/Dating/spiritual-request` | Publish the **accepted types and maximum file size** (Swagger documents neither), and say whether the file's content is checked or only its declared type | **Probed 2026-09-17 (`sp4@yopmail.com`, refused uploads only — nothing was created):** a wrong type gets `400 "Document type 'image/gif' is not allowed. Accepted: application/pdf, image/jpeg, image/png, application/msword, application/vnd.openxmlformats-officedocument.wordprocessingml.document."` The type is matched on the declared content type, before size: a 12 MB `text/plain` got the same type error. A 40 MB request is refused by the host itself (`413`, IIS text "request entity is too large"), so the cap is somewhere between. The app now checks on the device (PDF / JPG / PNG, **10 MB**, a guess) and rewords the server's type refusal; if the real limit differs, the app's number should change with it. Word documents are accepted by the server but not offered, since the screen promises PDF, JPG or PNG. | 🔴 Open |

<!-- Add new rows above this line. Keep the example row for reference. -->
